import {FormEvent,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {Logo} from '../components/Logo'; import {Button} from '../components/UI'; import {useAuth} from '../lib/auth';
export function Login(){const [u,setU]=useState(''),[p,setP]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const {login}=useAuth();const nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();setBusy(true);setErr('');try{await login(u,p);nav('/admin')}catch(e){setErr((e as Error).message)}finally{setBusy(false)}}return <div className="login-page"><div className="login-visual"><div className="orb o1"/><div className="orb o2"/><Logo/><h1>Play. Learn. Win.</h1><p>A private interactive quiz platform for Mahendra Engineering College.</p></div><div className="login-panel"><div className="login-box"><span className="eyebrow">COLLEGE QUIZ PLATFORM</span><h2>Welcome back</h2><p className="muted">Sign in to manage quizzes and live sessions.</p><form onSubmit={submit}><label>Username<input value={u} onChange={e=>setU(e.target.value)} autoComplete="username"/></label><label>Password<input type="password" value={p} onChange={e=>setP(e.target.value)} autoComplete="current-password"/></label>{err&&<div className="error">{err}</div>}<Button type="submit" disabled={busy}>{busy?'Signing in…':'Sign in'}</Button></form><small className="login-note">Initial development accounts are created by the Prisma seed script.</small></div></div></div>}
