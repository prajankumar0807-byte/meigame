import {useEffect,useState} from 'react'; import {api} from '../api/client'; import {Button,Card} from '../components/UI'; import {Plus,UserX} from 'lucide-react';
export function Users(){const [items,setItems]=useState<any[]>([]),[open,setOpen]=useState(false),[form,setForm]=useState({fullName:'',username:'',password:'',department:'',year:'',email:''});const load=()=>api<any>('/users').then(x=>setItems(x.items));useEffect(load,[]);const create=async()=>{await api('/users',{method:'POST',body:JSON.stringify({...form,year:form.year?Number(form.year):undefined})});setOpen(false);setForm({fullName:'',username:'',password:'',department:'',year:'',email:''});load()};const disable=async(id:string)=>{if(confirm('Deactivate this user?')){await api(`/users/${id}`,{method:'DELETE'});load()}};return <><header className="topbar"><div><span className="eyebrow">DIRECTORY</span><h1>Users</h1></div><Button onClick={()=>setOpen(true)}><Plus size={16}/>Add user</Button></header>{open&&<Card className="form-card"><h2>Create user</h2><div className="form-grid">{Object.entries(form).map(([k,v])=><label key={k}>{k}<input type={k==='password'?'password':'text'} value={v} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}</div><div className="actions"><Button variant="ghost" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={create}>Create</Button></div></Card>}<Card><div className="table-wrap"><table><thead><tr><th>Name</th><th>Username</th><th>Department</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>{items.map(u=><tr key={u.id}><td><strong>{u.fullName}</strong></td><td>{u.username}</td><td>{u.department||'—'}</td><td>{u.role}</td><td><span className={`badge ${u.isActive?'live':'ended'}`}>{u.isActive?'ACTIVE':'INACTIVE'}</span></td><td><button className="icon-btn" onClick={()=>disable(u.id)}><UserX size={16}/></button></td></tr>)}</tbody></table></div></Card></>}
