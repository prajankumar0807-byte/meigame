import {Card} from '../components/UI'; import {useAuth} from '../lib/auth'; export function UserDashboard(){const {user}=useAuth();return <><header className="topbar"><div><span className="eyebrow">MY SPACE</span><h1>Hello, {user?.fullName}</h1><p className="muted">Your personal MEIGAME dashboard.</p></div></header><Card><h2>Profile</h2><p>{user?.department||'Department not set'} {user?.year?`· Year ${user.year}`:''}</p><p className="muted">Your quiz history and results will appear here as you participate.</p></Card></>}
