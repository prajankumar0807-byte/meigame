import {useEffect,useState} from 'react'; import {api} from '../api/client'; import {Card} from '../components/UI';
export function Activity(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any>('/admin/activity').then(x=>setItems(x.items))},[]);return <><header className="topbar"><div><span className="eyebrow">AUDIT TRAIL</span><h1>Activity logs</h1></div></header><Card><div className="table-wrap"><table><thead><tr><th>Action</th><th>Actor</th><th>Target</th><th>When</th><th>IP</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.action.replaceAll('_',' ')}</strong></td><td>{x.user?.username||'Participant'}</td><td>{x.targetType||'—'}</td><td>{new Date(x.createdAt).toLocaleString()}</td><td>{x.ipAddress||'—'}</td></tr>)}</tbody></table></div></Card></>}
