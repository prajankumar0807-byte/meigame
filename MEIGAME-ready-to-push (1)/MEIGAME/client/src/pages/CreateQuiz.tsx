import {FormEvent,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {api} from '../api/client'; import {Button,Card} from '../components/UI';
export function CreateQuiz(){const [title,setTitle]=useState(''),[desc,setDesc]=useState(''),[subject,setSubject]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{const x=await api<any>('/quizzes',{method:'POST',body:JSON.stringify({title,description:desc,subject})});nav(`/admin/quizzes/${x.quiz.id}`)}catch(e){setErr((e as Error).message)}finally{setBusy(false)}}return <><header className="topbar"><div><span className="eyebrow">QUIZ STUDIO</span><h1>Create quiz</h1></div></header><Card className="form-card"><form onSubmit={submit}><label>Quiz title<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Data Structures — Unit 2" required/></label><label>Subject<input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Information Technology"/></label><label>Description<textarea value={desc} onChange={e=>setDesc(e.target.value)} rows={4} placeholder="What is this quiz about?"/></label>{err&&<div className="error">{err}</div>}<div className="actions"><Button variant="ghost" onClick={()=>nav('/admin/quizzes')}>Cancel</Button><Button type="submit" disabled={busy}>{busy?'Creating…':'Create quiz'}</Button></div></form></Card></>}
